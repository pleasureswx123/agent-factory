# 复制为 .env（runtime / drizzle-kit / web 共用）
DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5432/agentos

# Agent Runtime 服务地址（web 端直连）
NEXT_PUBLIC_RUNTIME_URL=http://127.0.0.1:4001

# Runtime 监听端口
PORT=4001

# 主密钥文件路径（可选；默认 ~/.agent-os/master.key，首次运行自动生成）
# AGENT_OS_MASTER_KEY_FILE=
